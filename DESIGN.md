# 设计说明

写给想改代码的人。只想用工具的话，看 [README.md](README.md) 就够了。

## 为什么不做「真·页底注」

Kindle 原生阅读器**没有**「脚注常驻页底」这个开关。脚注是弹窗、跳转还是普通文字，
完全由书籍自身的标记决定 —— 只要书里是 `<aside epub:type="footnote">`，渲染引擎就一定会做成弹窗。
这个行为写在 Amazon 的 KFX 阅读器里，KUAL 只能在系统里启动别的程序，改不了原生阅读器的渲染逻辑。

另外两条路也都堵死了：

1. Send to Kindle 推到设备上的书，到手已经是 **KFX**（亚马逊服务器转的），不再是 EPUB。
   所以「在设备上改 EPUB」这条路从一开始就不存在。
2. **KOReader 读不了 KFX**（只支持 EPUB / PDF / Mobi / FB2 等），
   所以「装 KOReader 再读推送的书」也不成立。

真正的「当前屏幕底部动态脚注」需要排版引擎在运行时按字号实时计算页高，只有 KOReader 做得到。

因此本工具走的是：**推送之前，在电脑上把注释搬到引用它的那段正文后面**。
实际体验接近页底注 —— 翻页时注释就在眼前，不点、不跳、不弹窗。

## 核心流程

`Converter.run()` 只有一条路径，没有模式分派：

1. 扫全书 XHTML，找出**注释定义**（`epub:type="footnote"` 或类名像 `footnote` / `fnote` / `noteContent` 的块级元素）。
2. 扫**正文角标**（带 `epub:type="noteref"` 的 `<a>`，或类名里含 `ref` / `marker` / `sub` / `sup` 的元素）。
3. 把定义和角标**配对**（见下）。
4. 把注释内容搬进正文，插在引用它的那个段落后面，抹掉 `epub:type` 语义。
5. 搬空的注释页自动从书里删掉（可用 `--no-drop-empty-file` 保留）。

## 配对策略：编号优先、链接兜底

这是全脚本最容易出错、也最值得小心的地方。很多书（多看 / 微信读书导出、Calibre 转的）链接本身就是乱的：

1. **注释条目没写 id**，回跳链接还是死的 `href="#"` —— 按链接找不到，注释就插不进去。
   应对：优先读取注释块**内部**回跳 `<a>` 的 id。
2. **角标指到了别的注释上** —— 角标 `[32]` 的 `href` 指向的内容其实是 `[20]` 那条。
   照着链接走，就会把**错误的注释**插进正文，比不处理还糟。
3. **注释定义和正文不在同一个 XHTML 文件** —— 支持按 `part0057.html#ch53` 的跨文件链接反查。

所以策略是：先按 id / 回跳链接找，找到之后还要比对「角标上的编号」和「注释正文开头的编号」是否一致；
一致才信，不一致就改按编号重新配。

实测某本 541 条注释的书，有 108 条注释**本身就没写链接**，全靠编号补配成功。

## 三个必须记住的坑

### 1. 类名判定要拆驼峰，但绝不能把角标类误判成定义

《四大名著-红楼梦[校注本]》用驼峰类名 `class="noteContent"`，脚注里的 `note` 后面紧跟大写 `C`，
只认独立单词的正则匹配不上。所以有了 `looks_like_note_class()`：把类名按驼峰 / 连字符 / 下划线切词再比对。

但反过来，带 `ref` / `marker` / `sub` / `sup` 的类名（`noteref`、`noteRef`、角标外层的 `subScript`）
必须判定为**正文角标**而非注释定义 —— 否则会把整段正文当注释搬走。
这是**静默且灾难性**的错误，书看起来没报错，内容却全乱了。

### 2. 不要用 `id(element)` 做元素身份

插入注释会改变文档结构，后续 XPath 会跟着变。代码里用临时属性 `data-kfxdefkey` 作为定义的稳定身份 token，
序列化前统一摘掉。

### 3. 嵌套 `<sup>` 会造成重复编号

原书写成 `<a><sup>(1)</sup></a>` 时，旧的改写逻辑会留下内层 `<sup>`，正文角标显示成两个 `(1)`。
现在会清空子元素并展平成单层上标。

## CSS 只用 em

注入的样式全部用 `em` 作单位，不写死 `px` / `height` / `position`。
所以 Kindle 上调字体、字号、行距，注释都会跟着缩放，不会被撑坏。

## 回归测试

`_test/` 里 6 个样本，覆盖：

| 样本 | 覆盖的结构 |
|---|---|
| `sample_a` / `sample_b` | 标准 EPUB3 脚注、注释单独一个文件 |
| `sample_c` | 不规范 HTML（未闭合 `<br>`、只有 `class="footnote"`） |
| `sample_d` | 同段多引用 + 编号去重 |
| `sample_e` | **链接错位的书** |
| `sample_f` | **驼峰类名 + 角标套 `subScript`** |

```bash
pip install lxml
python _test/run_tests.py          # 一键跑完：转换 + 结构校验 + 配对校验
python _test/run_tests.py --regen  # 先重新生成样本再跑
```

`run_tests.py` 会对 6 个样本各跑 `tight` / `compact` / `loose` 三档，输出到 `_test/out_*.epub`，
再交给 `verify_outputs.py`（结构）和 `check_pairing.py`（配对）校验。任一环节失败即非 0 退出。

改完配对逻辑，**务必**跑一遍 `check_pairing.py` —— 错配不会让脚本报错，只会让书悄悄变错。
