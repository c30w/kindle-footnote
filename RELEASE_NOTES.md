# v1.0.0 · 首个正式版

把 EPUB 里的**弹窗脚注 / 跳转尾注**，改写成**紧跟正文的内嵌注释**。
推送到 Kindle 之后，注释就印在引用它的那段正文下面 —— **不点、不跳、不弹窗**。

用原生 Kindle 阅读器就能看，不需要越狱，不需要装 KOReader，不改推送方式。

---

## 它解决什么问题

Kindle 原生阅读器**没有**「脚注常驻页底」的开关。脚注是弹窗、跳转还是普通文字，
完全由书籍自身的标记决定 —— 只要书里是 `<aside epub:type="footnote">`，
亚马逊的 KFX 渲染引擎就一定会做成弹窗。KUAL 只能在系统里启动别的程序，改不了渲染逻辑。

而且 Send to Kindle 推到设备上的书，到手已经是 KFX，不再是 EPUB，
所以「在设备上改书」这条路从一开始就不存在。

唯一可行的做法：**推送之前，在电脑上把书改好**。本工具就是干这个的。

## 怎么用（Windows，3 步）

1. 装好 Python 3.8+（装的时候勾 "Add Python to PATH"）
2. 下载 `kindle-footnote-v1.0.0.zip` 并解压
3. 把 `.epub` 拖到 `拖到这里转换(内嵌注).bat` 上

首次运行会自动建 `.venv` 并装 `lxml`，要联网，等几十秒。之后秒开。
输出 `原文件名-tight.epub`，**原书不会被改动**。可以一次拖多本。

命令行用户：

```bash
python epub_footnote_inline.py 书.epub --style tight --class-fallback
```

装成命令行工具：

```bash
pip install git+https://github.com/c30w/kindle-footnote.git
kindle-footnote 书.epub --style tight --class-fallback
```

三档紧凑度：`tight`（最省地方，bat 默认）/ `compact`（默认，带竖线）/ `loose`（注释长时用）。

## 这个版本能做什么

- **配对策略是「编号优先、链接兜底」**。很多书（多看 / 微信读书导出、Calibre 转的）链接本身就是乱的，
  照着链接走会把**错误的注释**插进正文，比不处理还糟。脚本先按 id / 回跳链接找，
  再比对角标编号与注释正文开头的编号是否一致，不一致就改按编号重新配。
- **自动识别多种脚注写法**：标准 EPUB3、老式 `class="fnote"`、驼峰类名 `noteContent`、
  多看 / 微信读书的 `duokan-footnote-item`、注释与正文分属不同 XHTML 文件。
- **自动清掉无语义包装**：注释里的 `ol` / `li` 会被拍平（否则每条前后多空一大截），
  注释开头的死链回跳 `<a>` 会被删掉（外链保留）。
- **注释块只用 `em` 作单位**，不写死 `px` / `height` / `position`，
  所以 Kindle 上调字体、字号、行距，注释都跟着缩放，不会撑坏。
- **原书只读**，输出到新文件。不加 DRM，不动封面、目录、字体。

## 实测覆盖

| 场景 | 注释数 | 结果 |
|---|---|---|
| 老式 `class="fnote"`（注释与正文分属不同 XHTML） | 6492 | 识别 6492 / 命中 6479 / 未匹配 13 |
| 驼峰类名 `class="noteContent"` | 2421 | 识别 2421 / 命中 2421 / 未匹配 0 |
| 真书（含无链接的注释条目） | 541 | 识别 541 / 命中 541 / 按编号补配 108 |
| 回归测试：6 个样本 × 3 档 | 18 个产物 | 结构校验全通过，配对错配 0 组 |

「未匹配」= 这条注释在正文里找不到引用它的角标，脚本**保持原样不动，不会乱猜**。

跑回归测试：

```bash
python _test/run_tests.py
```

## 已知限制

- **做不到严格意义上的「屏幕最底部」**。重排电子书没有固定页，页面是 Kindle 打开时按当前字号
  实时算出来的。本工具做到的是「注释紧跟引用它的那段正文」，翻页时就在眼前，
  实际体验接近页底注，但不是页底注。真正的动态页底注只有 KOReader 的排版引擎做得到，
  而它读不了推送的 KFX。
- **不处理 DRM**。本工具只做格式重排，请仅对你合法拥有的电子书使用。

## 依赖

Python 3.8+ 和一个 `lxml`（`lxml>=4.6,<6`）。没有别的。

## 校验信息

发布包 `kindle-footnote-v1.0.0.zip`（Windows）与 `kindle-footnote-v1.0.0.tar.gz`（Linux / macOS）
的 SHA256 校验和见本页面下方 Release 说明。

MIT License。
